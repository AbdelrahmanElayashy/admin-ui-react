import * as React from "react";
import { Card, CardHeader } from "@mui/material";

export default () => (
  <Card>
    <CardHeader
      style={{ textAlign: "center" }}
      title="Welcome to the customer management system"
    />
  </Card>
);
